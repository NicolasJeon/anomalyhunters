#include "EquipmentManager.h"

EquipmentManager::EquipmentManager(QObject* parent)
    : QObject(parent)
{
    // ── Practice #6 ──────────────────────────────────────────────────────────
    // TODO: 장비 데이터를 model_에 추가하세요 (아래 주석을 해제하세요)

    // model_.append({ "Pump Unit 1",    "10.0.0.11", true  });
    // model_.append({ "Pump Unit 2",    "10.0.0.12", false });
    // model_.append({ "Conveyor Belt",  "10.0.0.21", true  });
    // model_.append({ "Heat Exchanger", "10.0.0.31", false });
    // model_.append({ "Air Compressor", "10.0.0.41", true  });
}