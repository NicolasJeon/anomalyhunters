import QtQuick
import "../components"

// Detail panel — placeholder
Rectangle {
    color: Constant.bgWindow

    Text {
        anchors.centerIn: parent
        text:           "Detail"
        color:          Constant.textMuted
        font.pixelSize: 14
    }
}
