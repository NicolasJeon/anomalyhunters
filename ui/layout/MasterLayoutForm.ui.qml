import QtQuick
import "../components"

// Master panel — placeholder
Rectangle {
    color: Constant.bgPanel

    Text {
        anchors.centerIn: parent
        text:           "Master"
        color:          Constant.textMuted
        font.pixelSize: 14
    }
}
